# hardener-scheduler::lib
**File:** `crates/hardener-scheduler/src/lib.rs` | **Lines:** 24

## Purpose
Crate root. Re-exports all 6 modules and key public types: `SchedulerConfig`, `Daemon`, `ScanHistoryManager`, `JsonStore`, `NotificationDispatcher`, `ScanRunner`, `ScanSummary`, `TriggerType`, `SystemdGenerator`, `cron_to_calendar`.

## Dependencies
- All submodules
- Used by: CLI daemon/systemd commands, Tauri backend

## Flags
- None — clean.
