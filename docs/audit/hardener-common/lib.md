# hardener-common::lib
**File:** `crates/hardener-common/src/lib.rs` | **Lines:** 10

## Purpose
Crate root — declares the five public modules with module-level documentation.

## Modules
| Module | Description |
|--------|-------------|
| `binary_utils` | Safe binary resolution — searches TRUSTED_PATH (CWE-426 mitigation) |
| `error` | Workspace-wide error enum and Result alias |
| `file_utils` | Atomic file ops, config parsing, backup |
| `logging` | Tracing subscriber init |
| `types` | Re-exports from hardener-types |

## Flags
None — module doc added in prior audit.
